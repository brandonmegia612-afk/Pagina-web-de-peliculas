# Frontend

Aplicacion React creada con Create React App.

## Scripts

```bash
npm install
npm run dev
npm run build
```

Para produccion en Vercel, configura `REACT_APP_API_URL` con la URL publica del backend en Railway.
